/*
 * Auto-generated by scripts/alp_orchestrate.py -- do not edit.
 * Regenerate after changes to board.yaml `storage:`.
 */
/* clang-format off */

#include <zephyr/fs/fs.h>
#include <zephyr/storage/flash_map.h>

#include <zephyr/fs/littlefs.h>

FS_LITTLEFS_DECLARE_DEFAULT_CONFIG(alp_lfs_app_data);
static struct fs_mount_t alp_mnt_app_data = {
    .type = FS_LITTLEFS,
    .fs_data = &alp_lfs_app_data,
    .storage_dev = (void *)FIXED_PARTITION_ID(app_data_partition),
    .mnt_point = "/lfs/app",
};

FS_LITTLEFS_DECLARE_DEFAULT_CONFIG(alp_lfs_settings);
static struct fs_mount_t alp_mnt_settings = {
    .type = FS_LITTLEFS,
    .fs_data = &alp_lfs_settings,
    .storage_dev = (void *)FIXED_PARTITION_ID(settings_partition),
    .mnt_point = "/lfs/settings",
};

const struct fs_mount_t *alp_storage_mounts[] = {
    &alp_mnt_app_data,
    &alp_mnt_settings,
};
const size_t alp_storage_mount_count = 2;
/* clang-format on */
