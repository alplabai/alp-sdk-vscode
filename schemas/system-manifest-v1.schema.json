{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/alplabai/alp-sdk/metadata/schemas/system-manifest-v1.schema.json",
  "title": "ALP System Manifest v1",
  "description": "The single derived projection of a board.yaml that `west alp-emit system-manifest` (python -m alp_orchestrate --emit system-manifest) emits to build/system-manifest.yaml. It is the IDE / tool CONTRACT for a (possibly multi-image) project: one entry per per-core slice with its runtime, build directory, output artefact, and flash recipe, plus the IPC links and helper MCUs. Tools read THIS instead of re-deriving folder layout / build wiring from board.yaml + the SoM presets. Generated by the `alp_orchestrate` package (`python -m alp_orchestrate`); this schema and that emitter move in lockstep (a check gate validates the emitter output against this contract). Stability: schema_version 1 is additive-only (new optional fields only); a breaking change ships as schema_version 2 through a deprecation cycle, so consumers should tolerate unknown fields.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "generated_by", "hw_info", "slices", "ipc", "helper_mcus", "boot_order"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "generated_by": { "type": "string" },
    "hw_info": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sku", "som_hw_rev", "board_name", "board_hw_rev", "silicon"],
      "properties": {
        "sku": { "type": "string" },
        "som_hw_rev": { "type": ["string", "null"] },
        "board_name": { "type": ["string", "null"] },
        "board_hw_rev": { "type": ["string", "null"] },
        "silicon": { "type": ["string", "null"] },
        "eeprom": {
          "type": "object",
          "additionalProperties": false,
          "required": ["bus", "bus_id", "addr_7bit", "offset"],
          "description": "Resolved features.hw_info.eeprom manifest location, when declared in board.yaml.",
          "properties": {
            "bus": { "type": "string" },
            "bus_id": { "type": "integer", "minimum": 0 },
            "addr_7bit": { "type": "integer", "minimum": 80, "maximum": 87 },
            "offset": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "slices": {
      "type": "array",
      "description": "One entry per per-core image. core_id + os + the build/flash wiring the IDE needs to build, run, debug, and flash each image without re-deriving anything.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["core_id", "os", "status", "flash_method", "flash_args"],
        "properties": {
          "core_id": { "type": "string" },
          "os": { "type": "string", "description": "Resolved runtime (zephyr | yocto | baremetal | off); the value-set is owned by metadata/schemas/board.schema.json cores.<core>.os." },
          "app": { "type": "string", "description": "Relative path to this image's app source dir (binds the core to its sub-folder)." },
          "image": { "type": "string", "description": "Yocto image recipe (A-core stock images)." },
          "recipe": { "type": "string", "description": "Yocto bitbake recipe packaging this slice's `app:` source dir (required for an app-only Yocto slice; `app:` is a path, not a recipe name)." },
          "machine": { "type": "string", "description": "Yocto MACHINE." },
          "board": { "type": "string", "description": "Zephyr board target." },
          "toolchain": { "type": "string" },
          "build_dir": { "type": "string", "description": "Per-slice build directory (build/<core_id>-<os>); populated by fan-out." },
          "output_artefact": { "type": "string", "description": "Built image path; populated after a successful build." },
          "status": { "type": "string", "enum": ["pending", "ok", "failed", "skipped"] },
          "log_path": { "type": "string" },
          "reason": { "type": "string", "description": "Why a slice was skipped/failed." },
          "flash_method": { "type": "string", "description": "Backend the flasher dispatches to (e.g. zephyr_west_flash, yocto_wic_to_sd_or_emmc)." },
          "flash_args": { "type": "object", "description": "Method-specific flash arguments (shape varies by flash_method)." }
        }
      }
    },
    "ipc": {
      "type": "array",
      "description": "Inter-core links (rpmsg / mailbox) with their resolved carve-outs. A blocked link carries status:blocked + reason instead of the carve-out fields.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["name", "kind", "endpoints"],
        "properties": {
          "name": { "type": "string" },
          "kind": { "type": "string" },
          "endpoints": { "type": "array", "items": { "type": "string" } },
          "status": { "type": "string" },
          "reason": { "type": "string" }
        }
      }
    },
    "helper_mcus": {
      "type": "array",
      "description": "On-module helper MCUs (e.g. the GD32 bridge) that ship their own firmware image. Each key the SoM preset's `helper_firmware:` entry declares is projected here independently -- a helper may carry a field-update channel AND a local flash method at once (the GD32 bridge does), so a consumer must not read one key's presence as excluding another's.",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["name", "chip", "flash_policy"],
        "properties": {
          "name": { "type": "string" },
          "chip": { "type": "string" },
          "firmware_path": { "type": "string" },
          "flash_method": { "type": "string" },
          "flash_args": { "type": ["object", "string"], "description": "Method-specific flash arguments, or the string 'TBD' when a helper MCU's flash recipe isn't yet finalized in the SoM preset." },
          "flash_policy": { "type": "string", "enum": ["customer", "factory", "recovery_only"], "description": "WHO may invoke `flash_method`, and WHEN, projected verbatim from the SoM preset. REQUIRED -- there is no absent-means-`customer` default. `customer`: a plain flash target. `factory`: Alp Lab programs it in production; never a customer flash target. `recovery_only`: Alp Lab programs it in production and the customer may flash it ONLY to recover a bricked device, with Alp Lab-supplied binaries. Independent of `update_channel`." },
          "update_channel": { "type": "string", "description": "How this helper's firmware is updated in the FIELD (`alp_ota_spi_otp` for the CC3501E's own OTP, `alp_ota_spi_bridge` for the GD32 bridge's slot-A/B application bootloader). Deliberately unenumerated here: the SoM-preset schema owns the vocabulary, and this manifest is additive-only for consumers that must tolerate a channel added upstream. Its presence does NOT mean the helper has no flash method." }
        }
      }
    },
    "boot_order": {
      "type": "array",
      "description": "Resolved inter-image boot sequencing (empty until a SoM declares boot_order).",
      "items": { "type": "object" }
    },
    "storage": {
      "type": "array",
      "description": "Resolved storage partitions (present only when the project declares storage).",
      "items": { "type": "object", "additionalProperties": true }
    }
  }
}
